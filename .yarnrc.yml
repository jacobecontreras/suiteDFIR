# Electron and the electron-builder packaging step expect a real node_modules
# tree on disk; Yarn 4 would otherwise default to PnP and break them.
# Yarn cascades this file into electron/, which installs separately.
nodeLinker: node-modules
